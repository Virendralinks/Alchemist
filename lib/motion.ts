/** Drum-pad timings from Appendix A. MotionConfig collapsed these under reduced-motion. */
export const MOTION = {
  grab: 0.09,
  drop: 0.14,
  shake: 0.12,
  hover: 0.1,
  staggerItem: 0.02,
  rhymeTotal: 0.18,
} as const;
